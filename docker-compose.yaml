version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8022:8022"
    volumes:
      - ./data:/app/data
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
    restart: always

  streamlit:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8065:8065"
    volumes:
      - ./data:/app/data
    environment:
      - API_BASE_URL=http://api:8022
    depends_on:
      - api
    restart: always
